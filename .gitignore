# Kotlin/Gradle build artifacts
build/
.gradle/
.kotlin/
local.properties
*.class
*.jar
*.war
*.ear

# Keep the Gradle wrapper jar checked in so CI can run `./gradlew`.
!gradle/wrapper/gradle-wrapper.jar

# IDE / editor files
.idea/
*.iml
.vscode/
out/

# Node / JS / WASM working trees
kotlin-js-store/*
!kotlin-js-store/yarn.lock
!kotlin-js-store/wasm/
!kotlin-js-store/wasm/yarn.lock
.cache/

# Local working copy of the upstream Rust crate (reference only — never tracked).
tmp/

# OS cruft
.DS_Store
